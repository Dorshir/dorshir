#include "protocol.h"
#include <stdint.h> /* SIZE_MAX */

size_t Pack(char *_buffer, size_t _length)
{
    if (_buffer == NULL || _length == 0)
    {
        return SIZE_MAX;
    }



    return 0;
}

size_t UnPack(char *_packed, size_t _length, char *_buffer)
{
    if (_buffer == NULL || _buffer == NULL || _length == 0)
    {
        return SIZE_MAX;
    }


    

    return 0;
}
